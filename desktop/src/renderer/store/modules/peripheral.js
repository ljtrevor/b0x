const state = {
  devices: {}
}

const getters = {
  storeGetDevices (state) {
    return state.devices
  }
}

const mutations = {

}

const actions = {

}

export default {
  state,
  getters,
  mutations,
  actions
}
